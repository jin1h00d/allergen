<h1>Recipes List</h1>
<% @recipes.each do |recipe| %>
<%= recipe %>
<% end  %>

<%= form_tag(recipe_path, method: "get") do %>
  <%= label_tag(:title, "Search for:") %>
  <%= text_field_tag(params[:title]) %>
  <%= submit_tag("Search") %>
<% end %>
